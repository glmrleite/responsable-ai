# Modelo Ollama a usar (deve estar baixado localmente)
OLLAMA_MODEL=ollama/llama3.2

# URL base do Ollama (padrão quando rodando localmente ou via docker-compose)
OLLAMA_BASE_URL=http://localhost:11434
